name: CI

on:
  push:
    branches: [main]
    paths-ignore: ["**.md", "docs/**"]
  pull_request:

jobs:
  build:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode
        run: sudo xcode-select -s "$(ls -d /Applications/Xcode_26*.app 2>/dev/null | sort -V | tail -1 || ls -d /Applications/Xcode*.app | tail -1)"
      - name: Swift version
        run: swift --version
      - name: Unit tests
        run: swift test 2>&1 | tail -30
      - name: Build (release)
        run: swift build -c release 2>&1 | tail -20
      - name: Smoke test CLI
        run: .build/release/TokenBar --icon && .build/release/TokenBar --dump | head -5
